As discussed, the general rule is that page relevant components should be placed in the same folder as the page that uses them.

However, I have still created this components folder for top level components regarding the index page and the layout file.

Could also be used for globally used components